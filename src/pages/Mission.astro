---
import Layout from '../layouts/Layout.astro';
import {connectToDatabase } from '../server';
import StateCard from './../component/StateCard'
import {data} from '../data/state-mission';

// let data=[]
// let errorMessage='';
// let groupedData={};
// try {
//   const { db } = await connectToDatabase();
//   const collection = db.collection('plan');
//   data = await collection.find().toArray();

//   if (data.length === 0) {
//     errorMessage = 'No data found in the collection.';
//   } else {
//     // Group data by category
//     groupedData = data.reduce((acc, item) => {
//       if (!acc[item.Category]) {
//         acc[item.Category] = [];
//       }
//       acc[item.Category].push(item);
//       return acc;
//     }, {});
//   }
// } catch (error) {
//   errorMessage = `Error fetching data: ${error.message}`;
// }
---
<Layout title="Mission of Malaysian">
  <div>
    <div class = "flex justify-center items-center ">
      <h1 class = " items-center my-4 mx-auto justify-center flex text-3xl font-bold">
              Mission of Malaysian
      </h1>
      <div class = "mx-5 rounded-md bg-slate-700 p-2 text-white">
        <a href="/reward/">
          My reward
        </a>
      </div>
    </div>
    <main class = "mx-10 my-5 grid grid-cols-3">
      <a href="/Mission-sarawak/">
        <StateCard state="Sarawak" data={data}/>
      </a>
      <a href="/Mission-penang/">
        <StateCard state="Penang" data={data}/>
      </a>

    </main>
  </div>
</Layout>